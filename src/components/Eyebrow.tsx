export function Eyebrow({ children }: { children: string }) {
  return (
    <span className="mb-3.5 inline-block text-[13px] font-bold uppercase tracking-[0.12em] text-gold">
      {children}
    </span>
  )
}

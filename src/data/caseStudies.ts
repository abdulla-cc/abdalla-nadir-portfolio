export interface CaseStudy {
  id: string
  tag: string
  title: string
  overview: string
  problem: string
  dataset: string
  tools: string[]
  process: string
  insights: string
  output: string
  learned: string
}

export const caseStudies: Record<string, CaseStudy> = {
  'research-agent': {
    id: 'research-agent',
    tag: 'Featured · Jul 2026',
    title: 'Research Agent — Agentic RAG System over arXiv Papers',
    overview: 'An end-to-end retrieval-augmented generation system that answers questions about a corpus of arXiv papers with cited, source-grounded answers. Exposes two pipelines: a fast single-shot RAG endpoint for straightforward queries, and an agentic pipeline that decomposes complex questions into sub-questions, retrieves per sub-question, and runs a critic/verifier pass against the generated answer.',
    problem: 'A single-shot RAG call answers simple questions fine but breaks down on multi-part research questions, and there is no built-in check that the answer is actually grounded in what was retrieved — so a wrong or fabricated answer looks identical to a correct one unless someone manually checks the citations.',
    dataset: 'A corpus of arXiv papers, chunked and embedded for retrieval — cleaned with a conservative junk-filter that strips noisy PDF-extraction artifacts (reference lists, tables, title-page headers) before they can pollute retrieval.',
    tools: ['Python', 'FastAPI', 'Chroma', 'sentence-transformers', 'Groq (Llama 3.1)', 'Docker', 'Render'],
    process: 'Built the fast path first: embed and index paper chunks in Chroma, retrieve top-k on a query, and generate a cited answer in one LLM call via Groq. Layered an agentic pipeline on top for harder questions — decompose the question into sub-questions, retrieve separately for each, then run a critic/verifier pass that checks the drafted answer against its cited sources before returning it. Wrote a conservative junk-filter to catch noisy PDF-extracted chunks (reference lists, tables, title-page headers) that were diluting retrieval relevance. Production-hardened the service with input validation, structured logging, retry with exponential backoff on the LLM calls, a custom per-IP rate limiter, and 16 unit tests, then containerized it with Docker and deployed it live on Render.',
    insights: 'The junk-filter step made a measurable difference to retrieval relevance — a large share of what gets extracted from academic PDFs is reference lists, running headers, and table fragments that look like plausible text but carry no real signal, and filtering them out before embedding gave cleaner, more relevant retrieval hits. The critic/verifier pass on the agentic path also catches answers that drift from what the sources actually support, not just answers that are stylistically off.',
    output: 'A live, containerized FastAPI service on Render exposing both a fast single-shot RAG endpoint and an agentic decompose-retrieve-verify pipeline, with cited, source-grounded answers over the arXiv corpus, validated inputs, structured logs, backoff-protected retries, per-IP rate limiting, and a 16-test suite.',
    learned: 'End-to-end RAG and agent system design — not just "call an LLM with retrieved context" but decomposing questions, retrieving per sub-question, and verifying answers against sources. Practical LLM-app production engineering: what actually breaks in the wild (noisy PDF chunks, unbounded retries, unthrottled traffic) and how input validation, structured logging, retry/backoff, and rate limiting address each of those failure modes.',
  },
  'job-tracker': {
    id: 'job-tracker',
    tag: 'Full Stack / AI Engineering',
    title: 'JobTracker — AI-Powered Job Application Platform',
    overview: 'A live full-stack product that helps job seekers manage applications, analyze job descriptions, maintain a structured master CV, and generate role-specific CV versions from one secure workspace.',
    problem: 'A job search quickly becomes fragmented across spreadsheets, saved postings, notes, and multiple CV copies. The goal was to create one system that keeps the application pipeline organized while turning each job description into practical, honest guidance for improving the application.',
    dataset: 'User-created application records, job descriptions, and structured base-CV data. SQLModel provides a shared data layer across local SQLite development and managed PostgreSQL in production.',
    tools: ['React 19', 'Vite', 'FastAPI', 'SQLModel', 'PostgreSQL', 'JWT', 'Argon2id', 'Groq API', 'GPT-OSS 20B', 'Docker', 'Render', 'pytest'],
    process: 'Designed a protected React application with registration, login, route guards, a four-stage application board, an application form, a structured CV editor, job-description analysis, and CV tailoring. Built user-scoped FastAPI CRUD endpoints so every application and CV is isolated by the authenticated user. Added JWT authentication with Argon2id password hashing, validated structured AI responses with graceful error handling, containerized both services, and deployed the frontend, API, and PostgreSQL database through a Render Blueprint. Verified the backend with 53 tests covering authentication, user isolation, application CRUD, CV storage, and AI-service behavior.',
    insights: 'The most important engineering decisions were keeping AI credentials on the server, validating model output before returning it to the UI, and scoping every database query to the authenticated user. Returning the same not-found response for missing and unauthorized records also avoids leaking whether another user’s data exists.',
    output: 'A deployed React frontend and FastAPI API with secure accounts, a visual application pipeline, structured CV management, AI job-fit analysis, AI-assisted CV tailoring, interactive API documentation, and production PostgreSQL storage.',
    learned: 'End-to-end software delivery across frontend UX, API design, authentication, relational data modeling, LLM integration, automated testing, Docker, and cloud deployment — including how to turn an AI feature into a reliable product workflow instead of a standalone prompt.',
  },
  'budget-tracker': {
    id: 'budget-tracker',
    tag: 'PWA / Personal Finance',
    title: 'Budget Tracker — Offline-First Personal Finance PWA',
    overview: 'A mobile-first budgeting tool built around a simple personal workflow: enter a monthly allowance, divide it into categories, record each expense, and immediately see what remains. It works without an account and keeps financial records on the user’s own device.',
    problem: 'Spreadsheets were too tedious for day-to-day spending, while many budgeting apps required bank connections, subscriptions, accounts, or workflows that did not fit a simple Malaysian Ringgit allowance. The goal was a fast, private tool that answers one practical question: how much is left in each category this month?',
    dataset: 'A local IndexedDB database organized into months, budget categories, and transactions. Each transaction is assigned to a month and category, while categories can be separated into ordinary spending or protected savings.',
    tools: ['React 19', 'TypeScript', 'Vite', 'Tailwind CSS v4', 'Dexie.js', 'IndexedDB', 'React Router', 'date-fns', 'vite-plugin-pwa'],
    process: 'Designed five mobile screens for the dashboard, budget setup, expense history, savings, and month settings. Built a Dexie schema for months, categories, and transactions, then connected the UI with reactive live queries. Kept transactions as the source of truth and calculated total spent, remaining allowance, unallocated funds, category progress, and safe-to-spend-per-day values at render time. Added expense creation, editing, category reassignment, deletion, custom categories, savings contributions, month switching, and the option to copy a previous month’s budget. Finally, configured an auto-updating service worker and installable PWA manifest, then verified the production build and generated offline assets.',
    insights: 'Derived balances are safer than storing duplicate remaining-value fields: when a transaction changes or is deleted, every total recalculates from the ledger automatically. Treating savings categories separately also prevents protected money from inflating the daily spending recommendation.',
    output: 'An installable offline-first PWA with a responsive mobile interface, local-only financial storage, monthly budget planning, real-time category balances, expense history, savings progress, over-budget alerts, and a daily safe-spending calculation in Malaysian Ringgit.',
    learned: 'Offline-first application architecture, IndexedDB data modeling with Dexie, reactive queries, financial calculations from event history, responsive mobile UX, and PWA packaging with service-worker precaching.',
  },
  'decision-os': {
    id: 'decision-os',
    tag: 'AI Engineering / Decision Intelligence',
    title: 'DecisionOS — Evidence-Based AI Decision Support',
    overview: 'A local-first decision-support system that helps users compare two to four options using explicit criteria, external evidence, calibrated scoring, and structured reflection. It deliberately separates qualitative LLM reasoning from deterministic mathematics so the model can explain trade-offs without inventing probability figures.',
    problem: 'People often make important choices while affected by optimism bias, confirmation bias, incomplete information, or hidden constraints. Generic chatbots can reinforce that bias with confident-sounding advice and arbitrary percentages. DecisionOS was built to expose assumptions, challenge the preferred option, and make every numeric result reproducible and traceable.',
    dataset: 'User-created decisions containing a question, two to four options, context, constraints, criteria, and optional research evidence. The system also stores analysis versions, chat history, journal entries, chosen outcomes, satisfaction, and decision-quality ratings in SQLite locally or PostgreSQL when configured.',
    tools: ['Python', 'FastAPI', 'Pydantic', 'SQLAlchemy', 'SQLite / PostgreSQL', 'React 19', 'TypeScript', 'Expo', 'React Native', 'Groq / OpenAI-compatible LLMs', 'Tavily', 'Docker', 'pytest'],
    process: 'Built a six-stage pipeline: triage the decision and flag high-stakes cases; suggest and normalize weighted criteria; optionally research each option; evaluate options qualitatively on a 0–10 scale; calculate evidence-adjusted scores, softmax estimates, and confidence deterministically; then synthesize the result into a clear recommendation. Added follow-up questions when material context is missing, neutral fallbacks for omitted scores, source-quality classification, and a research-unavailable path that continues without fabricating citations. Extended the analysis with a Devil’s Advocate challenge, exact weight-only what-if rescoring, contextual chat, versioned history, outcome tracking, personal insights, JWT-capable APIs, React and Expo clients, and local one-command startup. Verified the backend with 168 passing unit and integration tests, the web production build, and the mobile TypeScript check.',
    insights: 'Probability and confidence answer different questions: the probability distribution ranks the available options, while confidence measures how much the system should trust that ranking based on evidence coverage, source quality, unknowns, and the gap between leading options. Keeping those calculations outside the LLM makes the output auditable and prevents persuasive language from masquerading as mathematical certainty.',
    output: 'An open-source local application with a FastAPI backend, responsive React web interface, Expo mobile client, decision wizard, research-backed analysis, deterministic probability estimates, high-stakes warnings, counterarguments, what-if scenarios, decision history, journals, outcome analytics, data export, and interactive API documentation.',
    learned: 'Designing reliable AI systems through strict separation of responsibilities: use language models for structured qualitative judgment, deterministic code for numeric claims, schema validation for model output, graceful degradation for unavailable services, and automated tests for the mathematical and API contracts.',
  },
  'retail-sales': {
    id: 'retail-sales',
    tag: 'Excel',
    title: 'Retail Sales Data Analysis',
    overview: 'An Excel-based retail sales analysis project focused on understanding sales, profit, quantity, and category performance using an interactive dashboard.',
    problem: 'Analyze retail sales performance and identify trends across regions, categories, and product segments to guide business decisions.',
    dataset: 'Superstore dataset with 9,800+ rows covering orders, products, regions, and customer segments.',
    tools: ['Excel', 'Pivot Tables', 'Slicers', 'Charts', 'Dashboard Design'],
    process: 'Cleaned the dataset, checked for missing values and inconsistencies, created pivot tables, analyzed sales/profit/quantity trends across all dimensions, and built an interactive Excel dashboard with KPI cards.',
    insights: 'Identified sales and profit patterns across regions, categories, and sub-categories. Technology was the highest-profit category; the West region led in sales.',
    output: 'Interactive Excel dashboard with slicers, KPI cards, bar charts, and visual summaries for business stakeholders.',
    learned: 'Improved skills in Excel data cleaning, pivot table analysis, dashboard design, and translating data findings into business insights.',
  },
  'hr-dashboard': {
    id: 'hr-dashboard',
    tag: 'Power BI',
    title: 'HR Analytics Dashboard',
    overview: 'A Power BI dashboard project focused on analyzing employee attrition and identifying high-risk employee groups across departments, job roles, and demographics.',
    problem: 'Understand employee attrition and identify the main factors linked to employees leaving, enabling HR to take targeted action.',
    dataset: 'IBM HR Analytics dataset with 1,470 employees and 35 features including job role, income, overtime, and satisfaction scores.',
    tools: ['Power BI', 'DAX', 'Excel'],
    process: 'Cleaned the data, created DAX measures for attrition rate and averages, built interactive visuals with slicers, and designed a professional dark-themed dashboard.',
    insights: 'Overall attrition rate was 16.12%. R&D had the highest attrition count. Overtime, job role, and age group were the most significant attrition drivers.',
    output: 'Interactive Power BI dashboard showing attrition by department, job role, gender, age group, overtime, and other employee factors.',
    learned: 'Improved Power BI skills, DAX measure writing, HR analytics understanding, dashboard layout, and data storytelling for business audiences.',
  },
  'student-db': {
    id: 'student-db',
    tag: 'SQL / Database',
    title: 'Student Management System',
    overview: 'A relational database project focused on managing students, courses, enrollments, and academic records using structured SQL design.',
    problem: 'Design a normalized database system to manage student records, courses, and enrollments with minimal redundancy and maximum data integrity.',
    dataset: 'Sample student, course, and enrollment records designed to simulate a real university database system.',
    tools: ['SQL', 'MySQL', 'Database Design', 'ERD'],
    process: 'Designed database tables, defined primary and foreign keys, established relationships, wrote SQL queries for CRUD operations, and tested all database interactions.',
    insights: 'A properly normalized database structure significantly reduces data duplication, improves query performance, and makes student records far easier to manage and scale.',
    output: 'SQL-based student management database with structured tables, relationships, constraints, and optimized queries.',
    learned: 'Improved SQL querying, relational database design, schema normalization, and understanding of how real-world systems store and retrieve data efficiently.',
  },
  'hr-pipeline': {
    id: 'hr-pipeline',
    tag: 'Python / Pipeline',
    title: 'HR Analytics Pipeline',
    overview: 'End-to-end HR analytics pipeline analyzing 1,470 employee records to identify attrition drivers using Python, SQL, and Power BI.',
    problem: 'Identify the key drivers of employee attrition in a retail company and build a system to flag high-risk employees before they leave.',
    dataset: 'IBM HR Employee Attrition dataset — 1,470 employees, 35 columns. Source: Kaggle.',
    tools: ['Python', 'Pandas', 'SQL', 'SQLite', 'Power BI', 'DAX', 'openpyxl'],
    process: 'Loaded and explored data → cleaned and encoded columns → engineered 3 new features (IncomePerYear, TenureRoleRatio, SeniorityScore) → built SQLite database with advanced SQL → identified 113 high-risk employees → auto-generated Excel report → built 2-page Power BI dashboard with 5 DAX measures.',
    insights: 'Sales Representatives earn the least ($2,626 avg) and leave at 39.76% — the highest attrition rate across all job roles.',
    output: '2-page Power BI dashboard — overview with navy theme and a dark neon risk analysis page with insight cards and high-risk employee flags.',
    learned: 'End-to-end data pipeline design, advanced SQL, DAX in Power BI, feature engineering, automated Excel reporting with openpyxl, and presenting findings to business stakeholders.',
  },
  scoms: {
    id: 'scoms',
    tag: 'AI / Final Year Project',
    title: 'AI-Powered Supply Chain Optimization & Risk Management (SCOMS)',
    overview: 'Final Year Project at Multimedia University (B.CS in AI). A three-module AI pipeline integrating LSTM demand forecasting, Random Forest supplier risk classification, and EOQ/ROP inventory optimization for retail supply chain decision support — surfaced through a Streamlit dashboard with a role-based recommendation flow for store managers.',
    problem: 'Retail supply-chain managers juggle forecasting, supplier risk, and inventory in disconnected tools. SCOMS unifies all three into a single AI-driven decision support system so a store manager sees demand, risk, and reorder thresholds in one place.',
    dataset: 'Retail supply-chain dataset covering historical demand, supplier performance, lead times, defect rates, and disruption history — supporting both time-series forecasting and supplier-level risk scoring.',
    tools: ['Python', 'TensorFlow / Keras', 'Scikit-learn', 'XGBoost', 'Streamlit', 'NumPy', 'Pandas'],
    process: 'Architected an LSTM forecasting model (2 stacked layers, 64 / 32 units, 30-day sliding window) for 7–30 day demand prediction; configured an XGBoost benchmark across RMSE, MAE, and MAPE. Built a Random Forest classifier (100 estimators) scoring suppliers as Low / Medium / High risk across five operational features — delivery delay rate, fulfillment rate, lead-time variance, defect rate, and disruption history. Implemented an inventory module computing EOQ, Reorder Point, and Safety Stock dynamically from the forecast output. Finalized system architecture and validated the dataset strategy through a 5-chapter FYP research phase, benchmarking against 11 peer-reviewed supply-chain optimization papers (2020–2026) to justify the hybrid LSTM + XGBoost approach over RNN and ARIMA baselines.',
    insights: 'Hybrid LSTM + XGBoost forecasting outperforms standalone RNN and ARIMA baselines across the surveyed literature. Combining supplier risk classification with dynamic inventory thresholds turns raw forecasts into actionable reorder decisions — not just numbers.',
    output: 'Streamlit dashboard with a role-based recommendation flow: demand forecasts, supplier risk tiers, and live EOQ / ROP / safety-stock thresholds on one screen — designed for non-technical store managers.',
    learned: 'End-to-end ML system design (research → architecture → implementation), hybrid model selection backed by empirical benchmarking, applied deep learning for time-series forecasting, and translating an academic FYP into a tool a non-technical user can actually use.',
  },
  diabetes: {
    id: 'diabetes',
    tag: 'Machine Learning',
    title: 'XGBoost Diabetes Risk Models — Clinical & Behavioral',
    overview: 'Two parallel XGBoost binary classification pipelines predicting diabetes onset — one on a 2,000-patient clinical dataset and another on a 13-feature behavioral / socioeconomic dataset. Built end-to-end in Python with rigorous handling of class imbalance, feature engineering, and 5-fold hyperparameter search.',
    problem: 'Predict diabetes onset in a clinically actionable way: the model has to catch the minority (positive) class rather than default to majority-class prediction, which is the failure mode of naive accuracy-optimised classifiers on imbalanced medical data.',
    dataset: 'Two parallel datasets — (1) 2,000-patient clinical with Glucose, BMI, BloodPressure, Insulin, and Age; (2) a 50 / 50 balanced 13-feature behavioral & socioeconomic dataset.',
    tools: ['Python', 'XGBoost', 'SMOTE', 'GridSearchCV', 'Scikit-learn', 'Pandas', 'NumPy'],
    process: 'Engineered 2 clinical interaction features (BMI × Age, Glucose / Insulin ratio) and applied median imputation across 5 columns (956 Insulin nulls, 573 SkinThickness nulls) before training. Resolved class imbalance via SMOTE oversampling applied exclusively to the training split — keeping the test distribution realistic. Executed 5-fold GridSearchCV across 432 hyperparameter combinations; optimal config was max_depth=7, n_estimators=300, lr=0.05. The behavioral pipeline used stratified train / test splits and StandardScaler normalization; both pipelines validated via confusion matrices on all splits.',
    insights: 'Applying SMOTE only on the training split lifted minority-class F1 to 0.76 — the clinical model stays viable for detecting at-risk patients rather than defaulting to majority-class. Clinical pipeline hit 82% test accuracy on the minority class; behavioral pipeline reached 79.4% accuracy with balanced precision / recall (0.79 / 0.80).',
    output: 'Two production-style XGBoost classifiers published as shareable Google Colab notebooks — one for clinical-feature input, one for behavioral / socioeconomic input, both with reproducible training pipelines and confusion-matrix validation.',
    learned: 'Disciplined class-imbalance handling (SMOTE on training only, never test), large-scale hyperparameter optimisation, interaction-feature engineering for clinical interpretability, and treating confusion matrices — not raw accuracy — as the real metric for medical ML.',
  },
  'yt-sentiment': {
    id: 'yt-sentiment',
    tag: 'NLP · Live Deployment',
    title: 'YouTube AI Sentiment Tracker',
    overview: 'A live, self-updating dashboard measuring public sentiment toward ChatGPT, Gemini, and Microsoft Copilot from real YouTube comments — scored by a RoBERTa transformer and refreshed automatically every Monday via GitHub Actions.',
    problem: 'Are people getting more negative toward AI tools as they go mainstream? Instead of guessing, this project measures it — one falsifiable anchor question answered with real data: "Has public sentiment toward AI tools gotten more negative over time?"',
    dataset: '650+ real YouTube comments collected from videos about ChatGPT, Gemini, and Microsoft Copilot via the YouTube Data API v3 (free tier). ~89% English after language detection; only English comments are sentiment-scored.',
    tools: ['Python', 'YouTube Data API v3', 'SQLite', 'HuggingFace Transformers (RoBERTa)', 'langdetect', 'pandas', 'Streamlit', 'Plotly', 'GitHub Actions'],
    process: 'Collect (paginated API pulls) → Store (SQLite with schema-level deduplication via PRIMARY KEY + INSERT OR IGNORE) → Detect language (langdetect) → Score sentiment (RoBERTa transformer: cardiffnlp/twitter-roberta-base-sentiment-latest) → Aggregate (confidence-weighted monthly scores: label direction × model confidence) → Visualize (Streamlit + Plotly) → Automate (GitHub Actions weekly refresh with repo write-back).',
    insights: 'YouTube commenter sentiment leans negative ~3:1 across all three AI tools, with Copilot trending most consistently negative in recent months (overall ≈ -0.18 on a -1 to +1 scale).',
    output: 'A deployed public Streamlit dashboard with KPI cards, an interactive monthly sentiment chart where dot size encodes sample size (data reliability shown honestly, not hidden), a viewer-controlled minimum-comments filter, real sample comments (most positive / negative per tool), and the raw monthly data table. The entire pipeline re-runs automatically every Monday via GitHub Actions — the dashboard stays live with zero manual work.',
    learned: 'End-to-end pipeline design, working with a real transformer model instead of rule-based sentiment tools, confidence-weighted aggregation, honest visualization of sparse data, and — most importantly — production skills a notebook never teaches: making scripts idempotent for automation, debugging a schema that worked locally but broke in CI, GitHub Actions permission scopes, and resolving merge conflicts with my own automation bot.',
  },
  'domain-expansion': {
    id: 'domain-expansion',
    tag: 'Computer Vision · Personal Project',
    title: 'Domain Expansion Classifier',
    overview: "Inspired by anime 'domain expansion' hand signs, this project recognizes different hand mudras through a webcam and reacts with custom particle and animation effects in real time — including a 'clash' mode where two people making different signs at once trigger a resolution based on a hierarchy.",
    problem: 'Anime power-ups look cool because the hand signs are precise and instantly recognizable — so the challenge was making a webcam actually tell them apart in real time, robustly enough to survive different hands, lighting, and backgrounds, without ever touching copyrighted anime footage.',
    dataset: 'A self-collected webcam dataset, hand-cropped using MediaPipe landmark detection across multiple gesture classes and lighting conditions.',
    tools: ['Python', 'PyTorch', 'torchvision (MobileNetV2)', 'OpenCV', 'MediaPipe', 'scikit-learn'],
    process: "Collected my own webcam training dataset and cropped it to hand regions using MediaPipe. Trained a CNN from scratch, then compared it against a fine-tuned MobileNetV2 (transfer learning) and documented the accuracy/speed tradeoffs. Built a real-time inference loop in OpenCV with confidence-based decision logic, designed original particle and animation effects per gesture, and extended the system to detect two people simultaneously via a custom hand-clustering heuristic — enabling multiplayer 'clash' interactions.",
    insights: "Hit suspiciously perfect training accuracy early on — turned out the model had learned to recognize my background, not my hand (a classic case of shortcut learning). Fixing it meant confronting real train/inference mismatches, not just tuning hyperparameters.",
    output: "A live webcam app: hold up a gesture, get an instant animated reaction; hold up two different ones with a friend, and watch the 'clash' logic decide who wins.",
    learned: 'Training and evaluating CNNs from scratch, transfer learning tradeoffs, building real-time computer vision pipelines, and debugging real ML failure modes like shortcut learning and train/inference mismatches — the kind of bugs a tutorial never prepares you for.',
  },
}
